node_modules/
*.log
.DS_Store
proxy-usage-stats.json
proxy-health.json
ollama-prep-state.json
wiki/.session-*
wiki/.cache/
docs/planning/review-manifests/
.clasp.json

# Defensive — see commit a0083bf and follow-up
temp_gas_prod/
.worktrees/

# Claude Code agent worktrees (locked, managed by `git worktree` —
# untracked otherwise; we don't want them polluting git status).
.claude/worktrees/

# Supervisor runtime state — supervisor system removed upstream (7b097ca);
# stale local copies may persist on disk, keep them out of status.
supervisor_journal.md
supervisor_state.md
supervisor_wiki.jsonl

# Runtime proxy logs and mcp-gas local state
proxy.stderr
proxy.stdout
.mcp-gas/

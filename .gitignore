node_modules/
*.log
.DS_Store
proxy-usage-stats.json
proxy-health.json
ollama-prep-state.json
wiki/.session-*
wiki/.cache/
docs/planning/review-manifests/
.clasp.json

# Defensive — see commit a0083bf and follow-up
temp_gas_prod/
.worktrees/

# Claude Code agent worktrees (locked, managed by `git worktree` —
# untracked otherwise; we don't want them polluting git status).
.claude/worktrees/

# Supervisor runtime state — actively written by tools/lib-memory.js,
# c-thru-state-marker.js, and c-thru-cleanup; local-only, never committed.
supervisor_journal.md
supervisor_state.md
supervisor_wiki.jsonl

# Proxy runtime logs and mcp-gas local state
proxy.stderr
proxy.stdout
.mcp-gas/

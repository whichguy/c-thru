---
name: Uplift-Cascade Pattern
type: entity
description: "Three-tier local→verify→decider cascade (accept|uplift|restart) generalized across implementer, reviewer-fix, test-writer, and planner roles — anchoring prevention via context omission on restart"
tags: [architecture, cascade, local-offload, confidence, anchoring, decider]
confidence: high
last_verified: 2026-04-21
created: 2026-04-21
last_updated: 2026-04-21
sources: [1558f542, a82cecbf, 22924d75, a183dfe6]
related: [planner-signals-design, capability-profile-model-layers, fallback-event-system, cascade-scope-contraction, self-recusal-chain, implementer-lint-directive]
---

# Uplift-Cascade Pattern

A three-tier decision cascade where a local worker produces output, verify.json gates on tests+syntax, and an `uplift-decider` (cloud judge, ~200 tokens) chooses accept/uplift/restart. The key design insight: **when verdict is `restart`, the cloud implementer never sees the local output** — anchoring is prevented by context omission, not by prompt hint. Generalizes across four agent roles (implementer, reviewer-fix, test-writer, planner) using a uniform decider schema with `context_kind` branching.

- **From Session 1558f542:** Research-grounded design: speculative cascades (Google, 2-3× speedup), GATEKEEPER/ReDAct (confidence-based deferral), CRITIC (tool-grounded critique beats self-reflection), "More Agents" (2-3 independent samples + vote, diminishing returns past 3), TACL survey (same-model self-correction is fragile — external critics/oracles are reliable). Deliberate no-gos: multi-agent debate (vote is simpler), self-reflection without tool grounding, N≥4 parallel samples, separate PM/Architect persona, replacing review-plan with one-shot `/speckit.analyze`.
- **From Session 1558f542:** Confidence-graded STATUS (E3): workers self-report `CONFIDENCE: high|medium|low` + `UNCERTAINTY_REASONS` with a backward-looking, evidence-based rubric. High = reused existing patterns + success_criteria map directly + can state satisfaction in one sentence each + made no assumptions. Medium = improvised pattern / interpreted criteria / guessed at API / added unneeded error handling. Low = unfamiliar domain / missing resources / ambiguous description / couldn't find calling site. Miscalibration tracked: over-confident (high but verify fails), under-confident (low but verify passes), uncertain (medium, passes half — actually correct state).
- **From Session 1558f542:** Anchoring prevention: `restart` verdict means the cloud implementer's prompt deliberately omits `local_output_path` — it solves from the digest alone. Grep-able invariant: `mode=clean prompts NEVER contain 'draft', 'prior', 'attempt', or 'previous'`. Enforced by unit test on orchestrator prompt builder, not just convention. Two decider biases guarded: always-restart (wastes local work, drives up cloud cost) and always-uplift (cloud defers to existing draft, anchoring persists). Decider prompt forces explicit criteria per verdict + one-line rationale.
- **From Session 1558f542:** Generalization across four agent roles via uniform decider schema with `context_kind: implementation|review|tests|plan`. Implementer cascade: local→verify→decider→{accept|uplift-cloud|restart-cloud}. Reviewer-fix cascade: after round 2 with no convergence, decider chooses {accept (cosmetic findings) | uplift-cloud (cloud reviewer continues with context) | restart-cloud (fresh from digest+source)}. Test-writer cascade: local test-writer→run tests→decider→{accept|uplift (add missing cases)|restart (redesign strategy)}. Planner cascade: already exists as dep_update/outcome_risk transition — the existing wave-transition logic is a prior instance of this pattern at a different granularity.
- **From Session 1558f542:** Converger as first-class logical role: new agent `converger` (local tier) with three invocation sites — (1) Phase 1 discovery reconciliation after explorer fan-out (flag disagreements, produce reconciled context), (2) multi-attempt selection for E4 best-of-N (compare N implementations, select winner by test-pass + majority similarity), (3) wave-summary aggregation (structured replacement for orchestrator's prose scan). Named and explicit makes convergence tunable (swap tier, cap timeouts) and the contract visible.
- **From Session 1558f542:** Implementation sequencing: E3 (confidence) → E1 (speculative cascade, now uplift-cascade) → E2 (converger) → E6 (test-first path) → E5 (local pre-reviewer, cheap 7B screen before reviewer-fix) → E4 (best-of-N with converger) → E7/E8 (situational). Each ≤1 wave. Measurement: `cloud_hops_per_plan`, `wall_clock_per_wave`, `reviewer_fix_rounds_per_item`, `human_escalation_count`. Keep only changes that move ≥1 metric 20%+ without degrading another. Per-wave cascade budget: `WAVE_CLOUD_ESCALATION_BUDGET=3` (default), prevents runaway cost.
- **From Session 1558f542:** Observability: `$wave_dir/cascade/<item-id>.jsonl` with structured events per decision step (local_worker, decider, cloud_worker) including verdict, rationale, duration. Per-wave rollup in `wave-summary.md` with stats (local_only_accepts, decider_invocations, verdict distribution, cloud_hops, budget_used). Enables tuning without reading individual traces.

- **From Session 1558f542:** Self-review (§19) identified 12 weaknesses in the cascade proposal. Most critical: W2 (CONFIDENCE is load-bearing but uncalibrated — if the signal is unreliable, cascade decisions are noise), W3 (decider adds net-new cloud hop; math unclear on whether cost offsets savings), W6 (premature generalization to 4 agent roles before implementer cascade is proven — YAGNI), W7 (anchor-prevention grep assertion fragile — use whitelist construction not denylist for clean-mode prompts). Result: scope contracted to Wave 1 only (CONFIDENCE field + logging hook) with explicit kill-switch gate. See [[cascade-scope-contraction]].
- **From Session 1558f542:** Wave-2 target architecture (build only if Wave-1 gate passes): every local worker output is a candidate draft; when verify fails or CONFIDENCE is low, `uplift-decider` (cloud judge, ~200 tokens) decides accept/uplift/restart. New agents: `uplift-decider.md`, `implementer-cloud.md` (two prompt modes), `converger.md` (aggregates parallel outputs). `escalation_policy` per item: `default | always_cloud | never_cloud`. `WAVE_CLOUD_ESCALATION_BUDGET=3` per wave. Migration: CONFIDENCE field optional for 1 release cycle (absent → treated as `medium`). Deferred expansion proposals catalogued as E2-E8 in plan Appendix B.

- **From Session a82cecbf:** Wave-2 cascade routing shipped (PR #34, 6 commits, 13 files, 684 lines). Implemented: self-recusal chain in all 4 worker agents (RECUSE STATUS + RECOMMEND field), Step 4b pre-dispatch classification + Step 5r recusal handling in orchestrator, 4 new agents (uplift-decider, implementer-cloud, test-writer-cloud, converger), 2 new capability aliases (deep-coder-cloud, code-analyst-cloud). Review-fix found 6 critical bugs: uplift-decider accept path missing in orchestrator, judge-tier sentinel not blocking non-dispatchable RECOMMEND values, converger missing self-recusal section, cloud worker agents missing improvement directives. All fixed in commits `cd4c0ac` + `63f0fea`.

- **From Session 22924d75:** Audit found test-writer-cloud doesn't branch `uplift` vs `restart` — it uses a single cloud path, breaking the anchoring-prevention invariant where restart mode must omit `local_output_path`. This means all cloud escalations from test-writer are effectively uplift mode, even when restart would be the correct verdict. Fix: add branching to test-writer-cloud dispatch (Batch A2 of the prompt audit plan).

- **From Session a183dfe6:** test-writer-cloud anchoring-prevention fix shipped (Batch A2): the agent now explicitly branches on `mode=uplift` (read `local_output_path`, produce patch), `mode=restart` (no local output path in context — fresh from digest), and `mode=direct` (no escalation context). This closes the gap identified in 22924d75 where all cloud escalations from test-writer were effectively uplift mode. Anchoring grep assertion added to `tools/c-thru-contract-check.sh` scoped to restart-mode sections only — not the full file — to avoid false-positives on prose describing the pattern.

→ See also: [[planner-signals-design]], [[agent-prompt-construction]], [[capability-profile-model-layers]], [[fallback-event-system]], [[declared-rewrites]], [[cascade-scope-contraction]], [[self-recursal-chain]], [[implementer-lint-directive]], [[dispatch-site-contract-gap]]
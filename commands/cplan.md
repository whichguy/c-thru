---
name: cplan
description: "Shortcut for /c-thru-plan — wave-based agentic planner (full multi-agent waves need CLI fleet inject)"
allowed-tools: "Skill"
---
Invoke Skill(skill="c-thru-plan").

Note: the skill files ship with the plugin, but multi-agent waves need the CLI
`cthru` install (`--agents` fleet). Plugin-only installs get routing/proxy only.

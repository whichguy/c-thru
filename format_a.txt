[C001 S 10.0] Payment gateway returns 502 Bad Gateway
  Resolves: Why is checkout failing for all users?
  +live     "Datadog logs show 100% 502 rate on /checkout"

[C002 D -10.0] Nginx reverse proxy is dropping backend connections
  Resolves: Is nginx correctly configured?
  -live     "systemctl status nginx shows active and routing correctly"

[C003 S 20.0] Node.js payment-service is in a crash loop
  Resolves: Is the backend stable?
  +live     "pm2 logs show payment-service restarting every 2s"
  +live     "Crash error: ECONNREFUSED to database.internal:5432"

[C004 D -10.0] Database server is unreachable on the internal network
  -live     "ping db.internal returns 0% packet loss"

[C005 S 10.0] Database credentials are invalid or rejected
  Resolves: Are the DB secrets correct?
  +live     "psql -U payment_user returns FATAL: password authentication failed"

[C006 D -10.0] AWS SecretsManager is unreachable or rate-limited
  -live     "aws secretsmanager get-secret-value works fine, no 429 errors"

[C007 S 16.0] DB password was rotated but service is using cached old credentials
  Resolves: Why are valid secrets failing?
  +artifact "AWS CloudTrail shows secret rotation event at 09:30Z today"
  +live     "pm2 show payment-service uptime is 14 days (started before rotation)"

[C008 S 10.0] Local .env.prod overrides the AWS Cloud Secrets
  Resolves: Where are the secrets coming from?
  +sus (4.0) "Service might be pulling from local file first"
  +artifact "Found .env.prod on the instance containing stale database password"

[C009 U -6.0] CI pipeline failed to clean up local env files on last deploy
  -artifact "GitHub Actions log shows 'Cleanup' step finished successfully"

[C010 S 10.0] A manual SSH session left a hidden backup file on the server
  +live     "ls -la found .env.prod.bak modified by user 'root' at 09:45Z"

[C011 C -3.0] Docker bridge network is isolated from DB vpc
  -sus (3.0) "Other containers on same bridge reach DB fine"

[C012 D -10.0] Internal DNS record for db.internal is stale
  -live     "dig +short db.internal returns correct cluster IP"

[C013 U -6.0] DB Security Group is blocking the new bridge CIDR
  -artifact "AWS SG rules allow all ingress from 10.0.0.0/16"

[C014 C -4.5] Application code is hardcoded to port 5432 and ignores config
  -sus (4.5) "I checked the code earlier, it definitely uses ConfigManager"

